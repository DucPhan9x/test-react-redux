import React from "react";
import ModalContacts from "../components/ModalContacts";

const Contacts = ({ title }) => {
  return <ModalContacts title={title}></ModalContacts>;
};
export default Contacts;
